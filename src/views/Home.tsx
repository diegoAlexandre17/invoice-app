import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      Home
      <Button variant="destructive">Destructive</Button>
    </div>
  );
};

export default Home;
